<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ReyNotes.Views.NotesEditView">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding Title}" FontSize="Title" />
            <Editor Text="{Binding Text}" VerticalOptions="FillAndExpand" />
            <Button Text="Save" Command="{Binding SaveNote}" HorizontalOptions="FillAndExpand" Margin="10"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>